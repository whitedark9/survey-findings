---
title: Survey Findings Charts
---

import { data } from "~/constants/data/chart";
import Visualization from "~/ui/visualization";

{Object.keys(data).map((key, index)=> {
return (

<Visualization key={key} data={data[key]} index={index} />) })}
