---
title: Survey Findings
---

import { sections } from "~/constants/data/sections";

<div className={"mb-10 text-center text-6xl font-bold"}>Survey Findings</div>

{Object.keys(sections).map((key)=> (

<div className="ms-20 mb-2 text-4xl font-bold capitalize" key={key}>
  {sections[key]}. {key}
</div>
))}
