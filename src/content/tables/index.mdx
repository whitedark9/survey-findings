import { data } from "~/constants/data";
import { Callout } from "nextra/components";
import clsx from "clsx";
import TableWithTitle from "~/ui/table-with-title";

# Survey Finding Tables

{Object.keys(data).map((key, index)=> {
return (

<TableWithTitle key={key} data={data[key]} index={index} />) })}
